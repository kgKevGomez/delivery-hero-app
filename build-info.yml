solutionName: DeliveryHeroApp.sln
# output:
#     artifacts: artifacts
#     publish: publish
#     projects:
#         - id: api
#           path: ./DeliveryHeroApp/Coolblue.CakeBuild.TYPE.csproj
#           generate-build-meta: true
#         - id: acceptance-tests
#           path: ./tests/Coolblue.CakeBuild.Tests.Acceptance/Coolblue.CakeBuild.Tests.Acceptance.csproj
#           octopus: false
test:
  unit: ./*.Tests.Unit.csproj
  # integration: ./**/*.Tests.Integration.csproj
  # acceptance: 
  #   glob: ./**/*.Tests.Acceptance.csproj
  #   packageName: acceptance-tests
environment:
  prefix: ENVIRONMENT_PREFIX_
  vars:
    APPGROUP: AWS_APPGROUP
    # TEAMCITY_URL: https://teamcity.coolblue.eu  #Keep this here for now, but later see if we can get an environment variable across all agents in TC
    # AWS_REGION: eu-west-1
  secrets:
    default: &default
    testing:
      <<: *default  #merge settings with default, you can override any key like ORACLE_PASSWORD below
    acceptance:
      <<: *default  #merge settings with default, you can override any key like ORACLE_PASSWORD below
    production:
      <<: *default  #merge settings with default, you can override any key like ORACLE_PASSWORD below